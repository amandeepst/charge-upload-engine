create table CM_MISC_BILL_ITEM
(
    MISC_BILL_ITEM_ID   VARCHAR2(12)  NOT NULL ENABLE,
    PARTY_ID            VARCHAR2(30)  NOT NULL ENABLE,
    LCP                 VARCHAR2(30)  NOT NULL ENABLE,
    SUB_ACCT            VARCHAR2(15)  NOT NULL ENABLE,
    CURRENCY_CD         CHAR(3)       NOT NULL ENABLE,
    STATUS              VARCHAR2(15)  NOT NULL ENABLE,
    PRODUCT_ID          VARCHAR2(30)  NOT NULL ENABLE,
    ADHOC_BILL_FLG      CHAR(1)       NOT NULL ENABLE,
    QTY                 NUMBER(36)    NOT NULL ENABLE,
    FASTEST_PAYMENT_FLG CHAR(1)       NOT NULL ENABLE,
    IND_PAYMENT_FLG     CHAR(1)       NOT NULL ENABLE,
    PAY_NARRATIVE       VARCHAR2(18)  NOT NULL ENABLE,
    REL_RESERVE_FLG     CHAR(1)       NOT NULL ENABLE,
    REL_WAF_FLG         CHAR(1)       NOT NULL ENABLE,
    CASE_ID             VARCHAR2(15),
    DEBT_DT             DATE,
    SOURCE_TYPE         VARCHAR2(30)  NOT NULL ENABLE,
    SOURCE_ID           VARCHAR2(15)  NOT NULL ENABLE,
    FREQUENCY_ID        VARCHAR2(15),
    CUTOFF_DT           DATE,
    VALID_FROM          DATE          NOT NULL ENABLE,
    VALID_TO            DATE,
    CRE_DTTM            DATE          NOT NULL ENABLE,
    ILM_DTTM            DATE          NOT NULL ENABLE,
    ILM_ARCH_SW         CHAR(1)       NOT NULL ENABLE,
    BATCH_CD            VARCHAR2(128) NOT NULL ENABLE
);
