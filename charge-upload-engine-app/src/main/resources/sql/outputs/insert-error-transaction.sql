insert /*+ :hints */ into error_transaction (txn_header_id,
                               per_id_nbr,
                               sa_type_cd,
                               retry_count,
                               reason,
                               code,
                               stack_trace,
                               created_at,
                               first_failure_at,
                               batch_code,
                               batch_attempt,
                               partition_id,
                               ilm_dt)
values (:txnHeaderId,
        :perIdNbr,
        :subAccountType,
        :retryCount,
        :reason,
        :code,
        :stackTrace,
        SYSTIMESTAMP,
        :firstFailureAt,
        :batch_code,
        :batch_attempt,
        :partition_id,
        :ilm_dt)
